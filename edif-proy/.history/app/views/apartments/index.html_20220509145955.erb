<h1>Apartments</h1>
<table>
    <thead>
        <tr>
            <th>Number</th>
            <th>Building</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
    <% @apartments.each do |apartment| %>
        <tr>
            <td><%= apartment.number %></td>
            <td><%= apartment.building %></td>
            <td><%=link_to 'Show', apartment%></td>
             <td><%= link_to 'Show', building_apartment_path(@building, @apartment) %>
              <td>
<%= link_to 'Edit', edit_building_apartment_path(@building, @apartment)
%>
<%= link_to 'Destroy', building_apartment_path(@building, @apartment) %>
<%= link_to 'New Apartment, new_building_apartment_path(@building) %>
        </tr>
    <% end %>
    </tbody>
</table>

