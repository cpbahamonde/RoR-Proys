<p style="color: green"><%= notice %></p>

<h1>Post tags</h1>

<div id="post_tags">
  <% @post_tags.each do |post_tag| %>
    <%= render post_tag %>
    <p>
      <%= link_to "Show this post tag", post_tag %>
    </p>
  <% end %>
</div>

<%= link_to "New post tag", new_post_tag_path %>


Nuestro nuevo archivo index.html.erb quedaría como sigue a continuación:
<p style="color: green"><%= notice %></p>
<h1>Post Tags</h1>
<div id="post_tags">
  <table>
    <thead>
      <tr>
      <td>Post ID</td>
      <td>Tag ID</td>
      <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
    <% @post_tags.each do |post_tag| %>
    <tr>
    <td><%= post_tag.post_id%></td>
    <td><%= post_tag.tag_id%></td>
    <td><%= link_to 'Show', post_tag %></td>
    <td><%= link_to 'Edit', edit_post_tag_path(post_tag) %></td>
    <td><%= link_to 'Destroy', post_tag, method: :delete, data: {
    confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
    </tbody>
  </table>
  <br>
</div>
<%= link_to 'New Post Tag', new_post_tag_path %>
