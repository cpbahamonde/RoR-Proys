<%= form_with(model: wine_oenologist) do |form| %>
  <% if wine_oenologist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wine_oenologist.errors.count, "error") %> prohibited this wine_oenologist from being saved:</h2>

      <ul>
        <% wine_oenologist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
